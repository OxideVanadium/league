{
  "devDependencies": {
    "gh-pages": "^6.2.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specidied\" && exit 1",
    "start": "parcel serve -d dist src/index.html",
    "build": "parcel build -d build --public-url /league/ src/index.html",
    "deploy": "gh-pages -d build"
  },
  "dependencies": {
    "parcel-bundler": "^1.12.5"
  }
}
